## Improving the queue

I killed the queue. Four hours in, I realized we never needed one —
the CDN's own retry behavior handles it better than anything I'd hand-roll.
That's $30 a month I'm not paying the rate-limit store for a problem that didn't exist.
