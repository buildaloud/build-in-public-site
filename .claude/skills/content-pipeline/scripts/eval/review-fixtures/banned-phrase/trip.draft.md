## Why the board is read-only

We shipped the ticket board with zero write endpoints. Nobody, not even the
project owner, can quietly edit the ticket history behind the AI's back — that's the whole point.
