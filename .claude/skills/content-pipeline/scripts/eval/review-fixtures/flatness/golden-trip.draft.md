## Cutting the filler

I re-read the flatness reviewer's own spec before shipping it, since it would be
extremely on-brand to ship a docs-empty draft under a docs-empty title. The rule
turned out to be one sentence once I stopped hedging it. Punctuation is
decoration. An empty sentence is the actual problem. I deleted six lines that
only restated the rule in new words and kept that one.
