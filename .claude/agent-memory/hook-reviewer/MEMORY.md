---
name: hook-reviewer
description: Precedent ledger for hook-reviewer — hooks/openers previously flagged and overruled, plus confirmed axis learnings.
---

## Precedents

- **2026-07-12** — When beat-1's `gateGuidance` explicitly mandates a verbatim protected line (e.g. "protect the exact framing '...'") plus a second admission/aside in the same beat (e.g. "this post was already stale by the time some of you read it"), don't flag the two-thread opener as diluting the hook's promise. That juxtaposition is an outline requirement, not drift — check `gateGuidance` for an explicit quote-protection instruction before flagging a hook for carrying more than one idea in the first 2-3 sentences. (automate-blog-writing-with-ai-agents rewrite)
