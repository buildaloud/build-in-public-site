---
name: formulaic-reviewer
description: Precedent ledger for formulaic-reviewer — constructions previously flagged as AI crutches and overruled, plus confirmed axis learnings.
---

## Precedents

- **2026-07-13 — "turn"-beat reveal phrased as negation is not a crutch when it's the outline's explicit gateGuidance.** In `automate-blog-writing-with-ai-agents`, the order-7 heading "The gate that isn't even a model" + opening line ("the gate ... is deterministic code. There's no LLM inside it.") mirrors the negative-parallelism shape, but the outline's own `gateGuidance` for that beat literally specifies the reveal as "the gate isn't even an LLM." When a `turn`/reveal beat's outline guidance names the negation as the intended surprise, don't flag it — flag only negation scaffolding that isn't outline-mandated (e.g. dismiss-then-reframe framing invented by the drafter, like "X was never the hard part... Y is the actual job", or a heading like "It's a loop, not a pass" with no outline mandate for that specific phrasing).
- **2026-07-15 — `grill-me-what-an-auditor-sees`: outline pre-mitigates crutches via explicit `gateGuidance`; check the draft against it before flagging.** This outline names its ONE flare-line negative-parallelism exemption explicitly ("You're not installing a snippet. You're putting an unread program in your agent's hands.", beat 2) and separately pre-instructs fixes for likely tricolons (reach lists must stay four items, not collapse to three; "write, exec, call out" gets "All in one run" appended to break the parallel; CTA recap must be two or four findings, never three). The draft applied all of these already. General lesson: when an outline's `gateGuidance` explicitly names a crutch risk and specifies the fix, check whether the draft already applied it before flagging — don't re-flag a construction the outline itself flagged and the drafter already neutralized.
- **2026-07-15 — `automate-blog-writing-with-ai-agents` rewrite: "roughly fifteen" reviewer-count phrasing is calibrated precision from facts, not hedging.** The outline's facts field itself uses "Roughly fifteen draft reviewers" because the exact count is genuinely fuzzy (axis groupings sum informally). Don't flag "roughly fifteen" / "roughly the fifteen" as a hedge crutch on this post — it's sourced uncertainty, not "it's worth noting"-style hedge theater. Also: anaphoric 3-item lists that enumerate genuinely distinct, outline-mandated facts (e.g. three named tone-gate detectors each with its own cap number, or three named binary link-integrity checks) read close to rule-of-three but are factual enumeration, not decorative tricolon — worth an elevation to vary the rhythm/break identical "(capped at N)" repetition, but not gate-worthy when every item carries required, distinct content.
- **2026-07-15 — `which-claude-model-to-use` outline: flare line "Burning Opus tokens on work Haiku finishes just as well is waste, not rigor." is the blessed flare exemption.** Per the axis's standing flare rule (never gate the designated flare for negative parallelism), don't re-flag this line across outline/draft review rounds on this post. Also: the hook/point/payoff triad "one Opus slot ... one Haiku slot ... Sonnet [running/holding] every job between them" recurs verbatim-ish across beats 1, hook, and 10 as an intentional callback (payoff beat literally instructs "Resolve the hook") — this is deliberate bookend structure per the post's `postFormula`, not accidental tricolon padding. Fine to leave as-is; only worth a rhythm-variation elevation on the third occurrence, never a gate.
- **2026-07-15 — `which-claude-model-to-use` order-9 (effort beat) closing clause "the one honest asterisk on this piece, not a hole I'm pretending isn't there" is outline-mandated, not drafter-invented.** The outline's own `gateGuidance` for order 9 spells out this exact negation-tail phrasing as the intended close. It reads as a textbook negative-parallelism tail, but since the outline authored it verbatim, treat it like the flare exemption: elevation only, never gate, on this specific clause in this beat. Distinct from the drafter-invented "X, not Y" tails elsewhere in round-1 drafts of this post (e.g. "not just my own rule of thumb, either", "not where it's merely describable", "not a lookup to confirm what's there", "not one call in isolation", "not a one-time win") — those five were NOT in the outline and were gated round 1 as a repeated-crutch pattern (same negation-tail construction stacked 5x across beats). Watch future drafts of this post for a relapse of that specific tail-negation habit; it seems to be this drafter's go-to intensifier on this piece.
- **2026-07-15 — `which-claude-model-to-use` order-7 (backdoor beat) "A security audit isn't checkable that way; it takes judgment to see what's missing" is also outline-mandated, same rule as order-9.** The order-7 `ourTake` in the outline contains this exact negative-parallelism-shaped sentence verbatim, and `gateGuidance` says to protect the surrounding closing lines nearly verbatim. Don't gate this clause when it appears in the draft. General rule confirmed twice now: when a beat's `ourTake` field itself is written with a negation-then-reframe shape and `gateGuidance` says to protect it near-verbatim, treat the rendered clause as outline-mandated (elevation at most), not a drafter crutch — check the outline's `ourTake` text before gating any negative-parallelism-shaped sentence on this post.
- **2026-07-15 — round-2 draft of this post relapsed into a different tell class: sales-speak reveal-framing ("That's the whole trick:", "Here's the honest part:") and a redundant "no exceptions" fragment tail used twice (order 3 and order 7).** These were NOT outline-mandated (outline's `ourTake` for those beats doesn't contain this phrasing) and were gated as a density violation (5 combined sales-speak/punch-fragment instances across the piece). If a future round overrules these as intentional voice, downgrade this entry to a precedent; until then, treat "That's the whole X:" / "Here's the honest part:" reveal-framing and doubled "no exceptions" tails as gate-worthy on this post.
- **2026-07-16 — `hired-a-team-of-specialists` round 1: two learnings.** (1) "X, not Y" (positive claim first, negated alternative second — e.g. "ship as part of Claude Code's own config, not a premium tier gated behind a paywall") is the same negative-parallelism crutch as "not X, it's Y" just reordered; gate it the same way when it isn't the outline's designated flare line. Confirmed by this outline's own beat-1 `gateGuidance`, which explicitly caps negative-parallelism at one use (the flare) for the WHOLE post — any second instance in either order is a violation, not just the canonical "isn't X, it's Y" phrasing. (2) The "That's [X]: [elaboration]" reveal-cadence recurred a third time on a third post ("That's the whole swap: one prompt becomes a handful of narrow ones...", beat 10/CTA) — now confirmed as a persistent drafter habit across posts, not tied to one piece; keep flagging every instance as sales-speak regardless of which post it shows up in.
- **2026-07-16 — `dividing-a-company-that-makes-no-money` outline is a clean-pass example of a mature, self-mitigating outline.** Nearly every beat's `gateGuidance` preemptively names the exact crutch risk for that beat and instructs the fix in-line (e.g. beat 2: "keep clear of a run of 'Not X.' punch fragments"; beat 4: "stay clear of a 'Not market. Not product.' fragment run"; beat 6/8: "no trailing negation coda" / "no tidy-bow reassurance about the future"). When an outline's `gateGuidance` already names and defuses a crutch, don't treat the underlying `intendedBeat`/`facts` prose as at-risk just because it lists several items or states a before/after contrast — check whether gateGuidance already covers it before hunting further. Also reconfirmed the rule-of-three precedent on this outline: the CTA sign-off "building Build Aloud in public at buildaloud.ai, what we're shipping lives at /projects, and the RSS feed has the rest" is a comma-joined "A, B, and C" tricolon shape but each item is a distinct required link (site/projects/RSS) — elevation only (recommend three short sentences instead of one comma list), never gate, matching the 2026-07-15 anaphoric-list precedent below.
- **2026-07-16 — `rate-limiting-an-llm-so-a-stranger-cant-run-up-my-bill` outline: flare line "When the hour rolls over, the window doesn't get reset. It ceases to exist." (order-8 beat, explicitly marked "FLARE LINE HERE" in the outline) is the blessed flare exemption for this post.** Don't gate it in future rounds. This outline is otherwise a mature, self-mitigating example (nearly every beat's `gateGuidance` preemptively names and defuses the exact old-draft negation/reveal-cadence it's replacing) — matches the `dividing-a-company-that-makes-no-money` pattern: don't re-flag content a beat's own `gateGuidance` already covers. Two genuine misses slipped through gateGuidance on the outline-review round: (1) order-6's asterisk sentence stacked two non-flare "X, not Y" negations in one sentence ("not before it starts" / "a soft edge, not a hole"); (2) order-3's "the service ran exactly as designed, fast and automated, with nobody around to get tired" is a three-part descriptive tail-stack (tricolon-shaped padding), distinct from the comma-tricolon regex shape but reads the same. Watch whether the draft round re-introduces either after the outline-round rewrite.
**Update 2026-07-16 (draft round):** partial fix. The "soft edge, not a hole" phrasing did NOT reappear (drafter dropped it cleanly). But the other asterisk negation persisted in mutated form: outline's own intendedBeat still carries "not just this one" and the draft rendered it as "not just the last one" — gated again in the draft round as a drafter-plus-outline-inherited trailing-negation tail. Also found two NEW non-outline-mandated negative-parallelism instances in order-6's input-cap bullet ("guards against a chatty visitor typing fast, not a caller hand-crafting API calls directly" and "the fix is this sentence, not new code" — outline's intendedBeat wrote these as two separate plain sentences, drafter converted both to "X, not Y" on its own), a permabanned "that's the whole problem" variant in order-3 ("which is the whole problem: nothing stopped it" — outline's own gateGuidance for order-1 names this exact shape as permabanned; treat it as permabanned everywhere, not just order-1), a "The honest reason:" sales-speak reveal-cadence in order-5 (same family as the confirmed "That's X: Y" / "Here's the honest part:" tic from other posts), and a drafter-added hedge in order-7 ("One more piece worth naming" vs. outline's plain "One more piece:"). This post's drafter's dominant failure mode is negate-then-reframe density (5+ instances per draft), not sales-speak — matches the pattern already seen on `ai-automation-stack` round 1, distinct from `which-claude-model-to-use`'s round-2 sales-speak relapse. Also confirms order-3's `gateGuidance` line "skip the old 'the downside isn't theoretical' negation opener" needs to be checked against PARAPHRASES, not just the literal banned string — draft opened with "The stakes here are documented, not hypothetical," a reworded dodge of the exact same banned opener.
**Update 2026-07-16 (later round): clean pass, all relapses fixed.** A subsequent draft resolved every item from the previous update: order-6 asterisk dropped "not just the last one" entirely (now "One honest asterisk covers every counter above" with no trailing negation); order-6 input-cap bullet split "guards against a chatty visitor... A caller hand-crafting API calls directly could still pad..." into two plain sentences (no "X, not Y"); order-3 opener is now "More than $82,000. That's what one developer's compromised AI API key generated..." (no "not hypothetical" paraphrase-dodge); order-3's descriptive tail collapsed to two items ("ran exactly as designed, fast and unattended" — not the old three-part "fast and automated, with nobody around to get tired"); order-5's "The honest reason:" sales-speak reveal-cadence is gone; order-7's hedge "One more piece worth naming" reverted to the outline's plain "One more piece:". Zero "That's [X]: [Y]" reveal-cadence instances anywhere in this draft. Flare line preserved correctly, echoed-in-different-words tail avoided (lands on "A fresh key gets created on the next request" per gateGuidance). CTA beat has exactly one link (chads.website), no second buildaloud.ai self-link. Confirms the fix pattern from the prior round holds under a fresh drafting pass — this post's drafter can produce a fully clean draft on this axis when the outline's `gateGuidance` is this explicit per-beat. No new gate findings this round.
- **2026-07-15 — cross-post confirmation: the "That's [keyword phrase]: [elaboration]" reveal-cadence is a recurring drafter tic, not a one-off.** Seen again in `ai-automation-stack` draft round 1, 3x ("That's the AI automation stack running this blog: four layers…", "That's the AI agent workflow automation running this blog end to end: twelve narrow jobs…", "That's AI agent workflow automation applied to compute instead of judgment calls: match the tool to the job…"). First instance of a defining term is fine; repeats of the identical "That's X: Y" opener across a post read as templated keyword-stuffing and should gate, matching the `which-claude-model-to-use` precedent above. Same post also independently relapsed into 5+ non-outline-mandated negative-parallelism instances in one draft ("not the model tier that sounds most impressive", "not an oversight left unfixed", "starts as a conversation, not a commit", "was never the twelve stages. It was deciding…", "no model discretion, just a scoring function", "no model call anywhere in the path, just a script…") — this drafter's default failure mode on THIS post is negate-then-reframe density, distinct from the sales-speak relapse seen on the other post. **Update 2026-07-15 (later round): a subsequent draft of `ai-automation-stack` fixed both relapses cleanly** — zero "That's X: Y" reveal-openers, zero non-outline-mandated negative-parallelism instances (ran the tone-grader's negParallel/tricolon/hedges/signposts/aiVocab/dramaticInversions/punchFragments/salesSpeak regexes manually against the full draft body: all zero except the two outline-mandated survivors already covered by the flare-exemption and ourTake-verbatim precedents above). The list-style fix that got it there: enumerations render as "A, B, C" with no trailing "and" before the last item (avoids the tricolon regex's `X, Y, and Z` shape) instead of collapsing to three matched clauses. Worth re-checking on every future round of this post, since it's relapsed twice already, but the fix pattern is now known-good — don't re-flag list-without-"and" enumerations here as tricolon.
