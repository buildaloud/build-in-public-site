---
name: wordsmith-reviewer
description: Precedent ledger for wordsmith-reviewer — word choices previously flagged and overruled, plus confirmed axis learnings.
---

## Precedents (index — see linked files for full detail)

- [filler-intensifiers.md](filler-intensifiers.md) — "actually"/"exactly"/"actual"/"genuinely"/"even" as throwaway intensifiers. Cross-post, standing per-draft grep every revision; carve-outs for real contrast, hard-constraint precision, and outline-baked text.
- [static-verb-sit.md](static-verb-sit.md) — "sit"/"sits"/"sitting" static-placement crutch. Cross-post, standing per-draft grep; carve-outs for deliberate callbacks and the idiomatic "sat around N" numeric sense.
- [tricolon-oxford-and.md](tricolon-oxford-and.md) — dropping the oxford "and" on 3-item lists is a deliberate Scout-voice device (don't flag); DO flag lists/triples that keep the "and." Two known repeat-offender sentences to check by name (Anthropic four-elements list in `automate-blog-writing-with-ai-agents`; alt-text CTA imperative triple).
- [repetition-patterns.md](repetition-patterns.md) — keyword-link modifier repeats, restated-sentence doubling (unless lampshaded as a callback), clustered "carries" crutch (3+ in one stretch), "X's own Y" citation tic (drop "own" on 2nd same-source mention), triple-repeated image variants (e.g. hand-rolled/hand-built/built-by-hand).
- [negparallel-and-outline-provenance.md](negparallel-and-outline-provenance.md) — drafter sometimes upgrades a safe outline paraphrase into a "not X, but Y" negParallel; check against outline's exact punctuation. General rule: flag weak words baked into outline `ourTake`/facts too, regardless of provenance — elevations are low-risk. Also: when a "one negParallel total" cap is set in one beat's gateGuidance, grep the whole draft — a second stray instance often lands in an unrelated later beat.
- [outline-protected-lines-drift.md](outline-protected-lines-drift.md) — when `gateGuidance` says a line must render "verbatim," the drafter still sometimes paraphrases it (swaps the one sharp word, or truncates a two-clause line to one). Standing check: grep every `gateGuidance` containing "verbatim" and diff word-for-word.
