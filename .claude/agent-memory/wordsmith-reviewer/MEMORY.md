---
name: wordsmith-reviewer
description: Precedent ledger for wordsmith-reviewer — word choices previously flagged and overruled, plus confirmed axis learnings.
---

## Precedents

- **"leverage"/"highest-leverage" can arrive baked into the outline's `ourTake` text verbatim** (e.g. "highest-leverage move" — 2026-07-13, `how-i-automate-blog-writing` draft). Still worth flagging as AI-vocab in elevations since it's a hard swap-list word, but expect it to survive if synthesis treats the outline's own phrasing as intentional — don't escalate to gate. Resolved as of the 2026-07-13 round-2 revision: "leverage" no longer appears anywhere in the draft — check freshly on each new revision rather than assuming it's still there.
- **Confirmed 2026-07-13, `how-i-automate-blog-writing` draft: the drafter deliberately drops the oxford "and" on short lists to dodge the tricolon AI-tell** (e.g. "the drafter, the fifteen critics, the deterministic gate" — no "and" before the last item). This is a genuine Scout-voice device, not an oversight — don't flag the asyndeton itself. Do still flag the rare list that keeps the "and" (breaks the pattern and trips `tone-grader.ts`'s tricolon regex). As of the 2026-07-13 round-2 revision, all previously-flagged lists (the Anthropic four-elements list, the structural-axes list, the integrity-layer list) have been fixed and hold the pattern correctly — re-check every multi-item list on every future revision, since this has slipped back in before.
- **New 2026-07-13 (round 2), `how-i-automate-blog-writing` draft: when a beat's assigned keyword is embedded as a markdown link (e.g. `[AI-audited agent skills](...)`), watch the sentences right after it for the same modifier repeating** — found "each one AI-audited before it gets listed... Exactly what gets AI-audited" stacking the phrase 3x across two sentences right after the link. Flag and vary the repeats; never touch the keyword phrase inside the link itself.
- **New 2026-07-13, `how-i-automate-blog-writing` draft: the drafter has a habit of inserting throwaway "actually" as an intensifier that adds no claim** (e.g. "the twelve stages...actually are", "is actually true", "actually get reconciled", "actually missing or broken", "the taxonomy actually catches") — check every revision for this specific filler word stacking up across the draft; cutting it is always a clean drop-in.
