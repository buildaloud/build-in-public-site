# Precedents — "How I Automate Blog Writing With AI Agents" (multi-agent-pipeline post)

- **Order 7 (tone-gate turn)** — `ourTake` aphorism "The gate I trust most is the one with no opinion." is the line that lands; protect verbatim, don't rewrite. H2-heading + closing-body-line echo of this line is a deliberate bookend device (reads *stronger*, not diluted) — do NOT flag double-occurrence as dilution. Only flag if the line is missing from BOTH the heading and the body prose (heading-only, no body occurrence, still counts as missing).
- **Order 6 (synthesis/loop-cap beat)** — current protected `ourTake` line: "I'd rather stall the whole run than ship something broken that looks finished." (supersedes earlier "referee fifteen conflicting opinions" wording, which drafting used to flatten to third person — no longer current). Confirmed stable across 2 consecutive draft passes as of 2026-07-15.
- **Order 3 (stale-description correction/turn)** — self-deprecating `ourTake` line "the most on-brand bug I could have shipped" now has protect-verbatim `gateGuidance`; confirmed stable, don't let it sand into a neutral "we updated the numbers" statement.
- **Order 11 (token-cost honest-limit beat)** — protected line "I pay that 15x bill on every post." confirmed stable in draft. (Earlier state had this buried third-person at the end of `intendedBeat` rather than in `ourTake` — now resolved.)
- **Order 13 (CTA, "Install the machine this post described")** — personal-agency clause "That's why I get to touch the draft least of anyone and still trust what ships" is STICKY: 3 confirmed drops across drafting rounds historically (swapped for meta-commentary about the callback structure itself), plus one outline-stage drop. 2 consecutive clean draft passes since — keep checking this exact clause every round regardless of a clean streak; do not treat one or two clean passes as resolved.
- **Orders 4 & 9 doubled trailing-sentence redundancy** — RESOLVED, did not recur next pass. Only re-flag if a draft reintroduces a flat restatement immediately after either beat's landing line.
- **Order 14 (Google spam-policy beat)** — watch item: drafting prone to restating the "generative AI as one example of the pattern" point twice in adjacent sentences, crowding out the beat's promised personal stake. Confirm a Scout-specific stake survives past the policy paraphrase.
- **General rule**: once a beat's protect-verbatim `gateGuidance` survives 2 consecutive outline passes unchanged, downgrade to informational/spot-check only — no longer needs elevating every pass unless the protect-verbatim clause itself disappears from a future outline.
