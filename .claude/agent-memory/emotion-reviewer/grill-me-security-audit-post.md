# Precedents — "grill me" security-audit post

- **Order 3 (Haiku-miss admission)** — confirmed clean in draft: "I tried Haiku once to save on cost and it missed a backdoor, so Sonnet's been the default ever since." survives as a real first-person sentence, not a bare fact/link. Only re-flag if a future draft flattens it back.
- **"What the audit ruled out" beat doubled sentence** — RESOLVED next draft pass (point now made once before the honest-limit close). Only re-flag if the doubled restatement reappears.
- **General pattern to keep checking on any post**: a protected `ourTake` line can survive intact but sit immediately after a near-duplicate sentence pulled from the beat's `intendedBeat` narrative — check for that shadow-duplicate specifically, not just presence of the protected line.
