# Precedents — "Alt Text for SEO and Accessibility Is One Job" post

- **Order 2** — protected line "That frame should have been the starting point from day one." confirmed stable across passes.
- **Order 3** — protected line "Image alt text best practices are one instruction, and I only had to write it once." confirmed stable.
- **Order 4** — two protected pieces: the sequencing line "How to write alt text for SEO comes down to sequencing." and the closing admission "I still haven't gone back and checked whether some of these mood-art hero images should carry no alt text at all." Both now have protect-verbatim `gateGuidance` (previously the admission was stranded in `facts` with no protection — RESOLVED). Order 8's closing-beat callback depends on this admission staying intact; if it sands into a flatter third-person restatement here, that callback has nothing to point to.
- **Order 5 (125-char myth)** — STICKY at outline stage: 4 consecutive outline passes with the near-miss admission ("nearly became a hard cap") sitting only in `intendedBeat`/`gateGuidance`, never pulled into `ourTake` itself as first person. Keep flagging at outline review with the concrete rewrite: "I almost baked a 125-character cap into the backfill rule on nothing but a number that kept getting cited with total confidence..." At DRAFT stage, risk is lower — drafting independently compensated twice in a row even without protect-verbatim guidance ("...cited confidently enough that it nearly became a hard cap I baked into the backfill rule. It isn't real.").
- **Order 6** — protected line "If I'm going to generate alt text with AI, the agent has to look at the actual pixels first." confirmed stable.
- **Order 8** — flare line "The moment I optimize an alt attribute for a keyword over the screen reader user hearing it, I've failed the only user that field exists to serve." must land as the literal final sentence; confirmed stable across 2 consecutive draft passes.
- **Order 7** — the "real X, real Y" adjacent-repetition watch item is RESOLVED: the 2026-07-15 re-draft replaced it with "gives the crawler honest words about an actual image" — no repeated word, payoff sentence reads clean. Retire the watch, no need to keep re-flagging.
- **Order 2, new pattern (2026-07-15)** — the fix-the-launch-bug sentence drifted passive ("It got fixed once alt text stopped being treated as an SEO field first...") in a beat that's otherwise first-person confessional throughout the rest of the post (I almost shipped, I baked into the rule, I still haven't checked). Flagged as an elevation with an active-voice, first-person rewrite ("I fixed it once I stopped treating alt text as an SEO field first..."). Watch for recurrence — passive voice undercutting agency in this one sentence specifically.
