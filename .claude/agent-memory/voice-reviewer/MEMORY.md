---
name: voice-reviewer
description: Precedent ledger for voice-reviewer — beats previously flagged as voiceless/flat and overruled, plus confirmed axis learnings.
---

## Precedents

(none yet)

## Axis learnings

- 2026-07-13 — the drafter frequently opens sentences with "That's the [X]" / "That's why [X]" as a summarizing connective (seen ~8-10x in a single ~1800-word post, e.g. "How I Automate Blog Writing With AI Agents"). Each instance reads fine in isolation and the instances are spread across sections, not consecutive, so it does NOT trigger the burstiness gate rule (which is about 3+ consecutive same-shape sentences). Treat repeated "That's X" openers as an elevation (vary 1-2 instances for texture), not a gate finding, unless you find an actual run of 3+ consecutive sentences sharing the construction.
