# Reference examples of a clean pass on this axis (detail file for MEMORY.md index)

## "grill-me-what-an-auditor-sees" post-fixes draft — calibration bar for "pass"
2026-07-15 (draft mode) — this draft (after prior-round fixes applied) is a strong reference example of the axis passing clean: hook paragraph runs 11/18/13/37/12/4-word sentences ending on the fragment "Nobody reads the skill.", every outline `ourTake` lands as a held opinion at or near verbatim ("A download count is social proof, and social proof audits nothing.", "A wide-open misuse surface is dangerous regardless of what the author meant.", the flare line "You're not installing a snippet. You're putting an unread program in your agent's hands." used exactly once as designated), and specifics (200,000 downloads, 3,984/36.82%/76, 0/35/85/4.6, AST-01/AST-02) all survive into prose untouched. Zero gate findings that round — keep this draft in mind as the calibration bar for what "pass" looks like on this axis.

## "which-claude-model-to-use" post-fixes draft — second clean-pass exemplar
2026-07-15 (draft mode) — after the two prior gate fixes on this slug (hook triad merged to 2 sentences, payoff-section Opus/Haiku/Sonnet triad merged to 2, both logged under Burstiness above), this round's draft holds both fixes and adds no new regressions: every outline `ourTake` and protected verbatim line ("Haiku missed a backdoor. That's the gap that killed the cheap-tier shortcut for anything judgment-shaped.", "Per-agent effort tuning is real money still sitting on the table, and I know it.", the flare line) survives intact, and specifics (the $5/$25, $3/$15 intro $2/$10, $1/$5, $10/$50 price grid; 1.75/one-finding vs 5.65/four-findings; the `~/.codex/bin/codex` path; 12/15 reviewer counts, 5-round caps) all made it into prose. Zero gate findings. One elevation-level pattern noted but not gated: two back-to-back 40+-word same-shape sentences (MindStudio example + security-team example in the "compounds" section) with no fragment break between them — flag as elevation, not gate, since the established burstiness-gate threshold is 3+ consecutive uniform sentences, not 2.

## "teaching-a-robot-to-balance-my-game" first draft — third clean-pass exemplar
2026-07-16 (draft mode) — first-round draft passed with zero gate findings, notable because it hit nearly every per-beat `gateGuidance` instruction on the first try rather than needing a fix-round: the turn beat ("Where the robot was wrong") landed both protected lines verbatim ("The math said fine. The hands said boring.", "The optimizer has no column for that." as the unglossed final sentence), kept the "butter" quote scoped to only that word with the gloss outside quote marks, kept both numbers exact (1.06 → 1.10, "halved" not "roughly half"), and worked the "tower defense balancing" phrase in as the opening sentence per instruction. The designated flare line ("The machine is a brilliant search-narrower and a terrible taste-maker") rendered verbatim exactly once. Three negation-tail bans (beat 1's "whole reason", beat 2's "not the screen", beat 5's "the gate isn't the story here / the rig is") were all avoided in favor of the positive-framing rewrites the outline specified. One elevation-level pattern noted but not gated: two consecutive ~30-32 word same-shape sentences opening "The surrogate" beat (prestige-bot-problem sentence + workaround-is-a-surrogate sentence), same as the established 2-sentence-is-elevation-not-gate threshold.
