# technical-honesty's footer-provenance and structural-mitigations asks recur every round, always declined

Confirmed 2026-07-15, "grill-me" DRAFT rounds 1 through 4. Two specific
technical-honesty elevations keep recurring, reworded but substantively
identical, on the same two spans: (1) a footer-disclosure clause
distinguishing the featured real audit's 0/35/85/4.6 example from an earlier
illustrative example used in a different post, and (2) an insert after "What
the audit ruled out" naming the audit's place in a bigger defense stack
(structural mitigations like signing, capability manifests, gateway-enforced
catalogs) beyond scoring/reading. Both have been raised, and declined,
identically in rounds 1-4. The reviewer's own language calls the mitigations
point "not a product gap serious enough to gate" each time it's raised —
it's judging its own finding as non-blocking even as it keeps re-proposing
it.

Keep dropping both by default as Chad's call, not auto-forwarded, unless the
underlying facts change (e.g. the featured example's provenance actually
becomes ambiguous, or the post adds an enforcement claim the mitigations
point would then contradict).
