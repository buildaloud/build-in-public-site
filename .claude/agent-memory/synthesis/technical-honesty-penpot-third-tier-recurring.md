# technical-honesty's Penpot third-tier (component tokens) elevation — recurring, never adopted

Confirmed 2026-07-15, "a-design-system-with-css-variables" draft round 1.
`technical-honesty`'s own finding text self-reports "check-5 mining, now open
across five consecutive passes": Penpot's cited article recommends a THIRD
tier (component tokens, e.g. `card.background -> theme.bg.surface ->
colors.white`) beyond this site's two-tier primitive/semantic split, and the
reviewer keeps proposing an insert-after tying that gap to the post's
hand-rolled exceptions (stale amber, gradient, SVG fill).

This is an elevation, not a gateFinding — never blocking — but it recurs
unresolved across five passes with the same content. Treat as a standing
for-consideration-only item: don't force it into the consolidated edit set,
and don't let it re-litigate the primitive/semantic paragraph the gate axes
(`flatness`/`formulaic`/`voice`) are already editing there — it collides with
gate-tier work on the same span more often than not and loses on tier alone.
If it ever gets independently corroborated by a second axis, revisit; until
then, keep surfacing it as "for your consideration" and stop treating each
recurrence as new signal.
