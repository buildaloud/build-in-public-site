# Stale reviewer input

A reviewer axis can silently be checking a stale/cached copy of the draft,
not the current one (2026-07-13 draft round #3, link-integrity). Pre-empt:
when a reviewer's quoted span doesn't literal-match the artifact text given
to synthesis, don't apply that finding — drop it with a one-line "quote
doesn't match current draft, likely stale reviewer input" note, but don't
discard the reviewer's VERDICT wholesale.
</content>
